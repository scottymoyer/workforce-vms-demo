{
  // Recommends the Datadog IDE extension (VS Code Marketplace + Open VSX, so Cursor
  // gets it too). It reads code-security.datadog.yaml and surfaces platform-synced
  // findings for the service.
  // NOTE: as of extension v2.36.0, local in-editor static analysis does NOT support
  // Java (its picker: Apex, C#, Go, JS, Kotlin, PHP, Python, Ruby, TS), so this Java
  // repo gets NO inline SAST squiggles — Java SAST runs in CI + the Datadog platform.
  // The analyzer engine supports Java; it's an extension enablement gap.
  "recommendations": [
    "Datadog.datadog-vscode"
  ]
}
